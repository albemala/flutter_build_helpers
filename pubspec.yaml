name: flutter_build_helpers
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  io: ^1.0.3
  glob: ^2.1.0
  pub_semver: ^2.1.1
  archive: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # linting
  very_good_analysis: ^3.0.1

flutter:
